import assert from 'assert';
import MyPromise from '../src/utils/MyPromise';



describe('MyPromise', function() {
	describe('emit event', function() {
		describe('emit event', function() {
			it('should emit event "on"', function() {
				//chain.on('one', () => {});

				console.log(new Promise());
			});
		});
	});
});